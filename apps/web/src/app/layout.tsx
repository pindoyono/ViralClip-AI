import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import { Providers } from "./providers";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "ViralClip AI — Turn Long Videos into Viral Short Clips",
  description:
    "Upload any long-form video and our AI automatically identifies the most viral moments, generates engaging clips, adds captions, and schedules posts across TikTok, Instagram Reels, and YouTube Shorts.",
  icons: { icon: "/favicon.ico" },
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={inter.className}>
        <Providers>{children}</Providers>
      </body>
    </html>
  );
}
